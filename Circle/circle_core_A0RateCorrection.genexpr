circle_getRateCorrectionFactor(baseFreq, freqRatio, binIndex)
{
	rateCorrectionAmount = 0.4; // hardcoded here so it matches across the project

	return baseFreq * pow(freqRatio, binIndex * rateCorrectionAmount);

}